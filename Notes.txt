vcpkg x-update-baseline --add-initial-baseline

conan profile detect --name gcc-debug
conan install . --output-folder=build --build=missing --profile gcc-debug
cmake -B build/ -DCMAKE_TOOLCHAIN_FILE=build/conan_toolchain.cmake -DCMAKE_BUILD_TYPE=Debug
cmake --build build/

conan lock create conanfile.txt

protoc --proto_path=. --cpp_out=. finance.proto

pip install mypy-protobuf
protoc --proto_path=. --python_out=. --mypy_out=. finance.proto


pacman -S mingw-w64-ucrt-x86_64-python-lxml
# Create a venv that can see system packages
python -m venv --system-site-packages .venv

# Activate the new environment
source .venv/bin/activate

# The prompt should now start with (venv)
pip install gcovr

gcovr -r . --filter 'src/greeter/' --filter 'src/app/' --html 'build/coverage.html'

